pipeline {
    agent any
    environment {

    }
    stages {
        stage("'Build' Compile & Package") {
            sh 'mvn clean package'
        }

        stage('Build Docker image') {
            
        }
        
    }
}